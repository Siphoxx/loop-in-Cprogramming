
//includes the standard input/out library for c to ble able to recieve and output data.

#include <stdio.h>

//The main entrance point is the main function.
 int main() {

// declare thew data type as integer and the variable as i
int i;

//i=0: for loop settings, inital value of variable i is 0.
//i<10: trun the loop as long as the i variable has value less than 10, so up to 9.
//i++:keep adding one to the current value of variable i.
for(i=0; i<10; i++){
//show this
  printf("I am number: %d\n",i);
}
 }
